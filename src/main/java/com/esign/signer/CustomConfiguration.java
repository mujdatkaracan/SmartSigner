package com.esign.signer;
  
import org.springframework.context.annotation.Configuration; 

@Configuration  
public class CustomConfiguration {
	/*  @Autowired
	    BeanFactory beanFactory;
	 
	 @Bean
	    public CertificateServiceImpl beanB() {
	        beanFactory.getBean("crtImpl");
	        return new CertificateServiceImpl();
	    }
	@Bean
	 public SampleBase getSampleBase() {
		 return new SampleBase();  
	 }
	 @Bean
	 public CertificateServiceImpl getDataSource() {
		 return new CertificateServiceImpl();  
	 }
	 
	 */
}
